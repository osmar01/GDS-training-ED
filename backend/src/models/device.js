
class Device {

  constructor(id, category, color, partNumber, ){
    this.id - id,
    this.category = category,
    this.color = color,
    this.partNumber = partNumber
  }
}

module.exports = Device
